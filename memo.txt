最初に、
inputsにパラメータを列挙する

出力は最後のツールの出力３つ

ステップに、idとrun(cwlファイル)をかく


in:
こちらは、cwlのファイルから入力をもってきて
id: cwlのファイルにある入力
out:
id:
source: こっちは、どこからもってくるかを書く。前のステップのoutからなど
決め打ちしたいときは、
genes


途中のステップで出力されるのがディレクトリ、その中にファイルもあるケース
出力されるディレクトリの中にあるファイルを指定したい

可能であればツール定義の方で、ディレクトリで出力をせずに、ファイルごとに取れるようにしておく。

ただし、ディレクトリで受けて、ディレクトリをつぎのに渡すようなときは、わざわざファイル単位にする必要はない
